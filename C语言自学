//输入密码，三次失败退出
int main()
{
	int i = 0;
	char password[20] = { 0 };
	for (i = 0; i < 3; i++)
	{
		
		printf("请输入密码");
		scanf("%s", &password);
		if (strcmp(password,"jiangshihao672")==0)

		{
			printf("密码正确");
			break;
		}
		else
		{
			printf("请重新输入");

		}


	}
	return 0;
}
